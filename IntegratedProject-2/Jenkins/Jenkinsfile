pipeline {
    agent any
    environment {
    }
    stages {
        stage("Checkout Code") {
            steps {
                checkout scm
            }
        }
        stage("Build Docker Image") {
            steps {
                sh "docker build -t writeto shell:${BUILD_NUMBER} ."
            }
        }
    }
}