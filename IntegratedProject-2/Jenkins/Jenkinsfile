pipeline {
    agent any
    environment {
		name="NAME"
    }
    stages {
        stage("Checkout Code") {
            steps {
                checkout scm
            }
        }
        stage("Build Docker Image") {
            steps {
				sh "pwd"
				sh "IntegratedProject-2"
				sh "ls -la"
                sh "docker build -t writetoshell:${BUILD_NUMBER} ."
            }
        }
    }
}